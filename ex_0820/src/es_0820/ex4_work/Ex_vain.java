package es_0820.ex4_work;

public class Ex_vain {

    private Can[] cans = new cans(5);
    private int moneny;

    //자판기에서 관리할 음료수를 준비
    public Vending(){}
        can [0] = new Can(name "환타", price : 1000);
        can [1] = new Can(mane "콜라", price : 1000);
        can [2] = new Can("")

    //상ㅇ자의 돈을 받는다
    //이 돈으로 먹을 수 ㅣㅇㅆ는 음료 목록만 보여주고    
    public void showDrink (int moneny)
    }
        if (cans[i];getPrice; i++){

            System.out.printf("%d, %");
        }
        for ( int i =0 ; i < cans.length; i++);
   
        if (cans[i].getPrice() <= money)
            ven.init();

            ven.showDrink(money);
   
    } //for
    
        this.money=money;
    


);

//사용자가 음료수를 선택하면
// 해당 음료수를 제공하고 잔돈을 돌려준다
public void dispens( String name){

    for( int i=0; i< cans.length; i++ ){

        if(cans[i].getName().equals(name)){
            System.out.println( name + "을(를) 선택");
            moneny -= cans[1].getPrice();//잔돈계산
        }
    }//dispens


}//main

    
}
